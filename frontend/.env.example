# ============================================================
# AlgoEdge Frontend — Environment Configuration
# Copy this to .env and fill in your backend URL
# ============================================================

# Default backend URL (user can override in Settings > Connection)
VITE_DEFAULT_BACKEND_URL=http://localhost:8000
